; vitepress-plugin-mermaid@2.0.17 declares peer vitepress: ^1.0.0 but works at
; runtime with vitepress 2.0.0-alpha.x (build verified). Remove this once the
; plugin publishes a version with a relaxed peer constraint.
legacy-peer-deps=true
